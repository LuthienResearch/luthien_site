<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}{{ site.title }} - {{ site.description }}</title>
    <link rel="stylesheet" href="{{ '/assets/css/styles.css' | url }}">
</head>
<body>
    {% include "navigation.njk" %}
    
    <div class="container">
        {{ content | safe }}
    </div>
</body>
</html>